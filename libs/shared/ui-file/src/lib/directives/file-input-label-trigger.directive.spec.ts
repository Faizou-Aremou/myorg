import { FileInputLabelTriggerDirective } from './file-input-label-trigger.directive';

describe('FileInputLabelTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new FileInputLabelTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
