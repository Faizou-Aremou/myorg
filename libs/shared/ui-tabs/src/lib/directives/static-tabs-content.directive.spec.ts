import { StaticTabsContentDirective } from './static-tabs-content.directive';

describe('StaticTabsContentDirective', () => {
  it('should create an instance', () => {
    const directive = new StaticTabsContentDirective();
    expect(directive).toBeTruthy();
  });
});
